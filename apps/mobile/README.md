# apps/mobile

React Native + TypeScript + Expo 앱은 이 폴더에 생성합니다.

초기화 명령:

```bash
npm create expo@latest apps/mobile -- --template
```

추천:
- TypeScript template 선택
- Expo Router 사용 여부는 라우팅 전략 확정 후 선택
